{
  "name": "bookscope-web",
  "private": true,
  "version": "1.6.0",
  "type": "module",
  "description": "BookScope r1 — 最小前端：上传书 + 问答 + 看带原文引用的答案",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:demo": "tsc -b && vite build --mode demo",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.2.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.7.0",
    "vite": "^7.3.5"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
